<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <title>News | Techlore</title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <%- include('./partials/head', {page: "news"}) %>
</head>
<body>
    <%- include('./partials/navbar', {page: "news", user: session.user}) %>
         <div class="news_container">
            <h1 class="news_tittle">News</h1>
            <% for (let i = 0; i < news.length; i++) { %>
                <%let thenews = news[i]%>
                    <%let date = new Date(thenews.date)%>
                    <%let month = `${date.getMonth()+1}`%>
                    <%if(date.getMonth()+1 < 10){ %>
                        <% month = `0${date.getMonth()+1}` %>
                    <%}%>
                    <div class="news_card">
                        <h2><%-thenews.title%></h2>
                        <p><%-thenews.description%></p>
                        <p class="time"> <span>-</span> Published on <time><%-(date.getFullYear()).toString().slice(2)%>/<%-month%>/<%-date.getDate()%></time> By <time><%-thenews.author%></time> <span>-</span> </p>
                    </div>
            <%}%>
             </div>
        <%- include('./partials/footer') %>
</body>
</html>
            